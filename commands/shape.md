---
description: Shape a new feature at the product level. Product questions only, never implementation.
argument-hint: "[the idea, in a sentence]"
---
Run the `shape-idea` skill on: $ARGUMENTS

Write both results to `docs/issues/<effort>/`: `interview.md` with the
questions, answers and numbered rounds, and `shape.md` with the shape.
